<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-2 sm:px-4 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="flex items-center px-2 lg:px-0">
        <div class="flex-shrink-0">
          <%=link_to root_path do%>
            <%= image_tag "Logo_Trello.svg.png", class: "mx-auto h-12 w-auto"%>
          <% end %>
        </div>
        <div class="hidden lg:ml-6 lg:block">
        <% if user_signed_in? %>
          <div class="flex space-x-4">
            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->

            <%= link_to "Dashboar", root_path, class: "rounded-md bg-gray-900 px-3 py-2 text-sm font-medium text-white" %>
            <div class=" justif-aling">
              <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white" %>
            </div>
          </div>
        <% end %>
        </div>
      </div>
      <% if user_signed_in? %>
        <div class="hidden lg:ml-4 lg:block">
          <div class="flex items-center">
           <%= link_to "New board", new_board_path, class: "block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500  focus-visible:outline-indigo-600" %>
            <% else%>
              <div class="absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none" role="menu" aria-orientation="vertical" aria-labelledby="user-menu-button" tabindex="-1">
                <%= link_to "sign_in", new_user_session_path , class: "text-white tracking-wide "%>
                <%= link_to "sign_up", new_user_registration_path, class: "text-white tracking-wide p-3"%>
              </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
